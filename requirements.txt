FROM python:3.9

WORKDIR /demo

COPY ./requirements.txt /demo/requirements.txt

RUN pip install --no-cache-dir --upgrade -r requirements.txt

COPY . .

CMD ["uvicorn", "demo.main:app", "--host", "0.0.0.0", "--port", "7860"]